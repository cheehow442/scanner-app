scripts:
  - name: Get Flutter packages
    script: |
      cd scannerV2/scannerV2
      flutter pub get

  - name: Build IPA
    script: |
      cd scannerV2/scannerV2
      flutter build ipa --release --no-codesign

artifacts:
  - scannerV2/scannerV2/build/ios/ipa/*.ipa
